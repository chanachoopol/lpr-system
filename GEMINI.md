# 🛡️ Project Development Rules & Safety Guidelines

## 1. การขออนุญาตก่อนแก้ไขโค้ด (Strict Approval Required)
- **ห้ามแก้ไข สร้าง หรือลบไฟล์ใดๆ ในโปรเจกต์โดยพลการเด็ดขาด**
- ก่อนจะแก้ไขอะไรก็ตาม ให้ปฏิบัติตามขั้นตอนนี้เสมอ:
  1. อธิบายปัญหาและสาเหตุให้ผู้ใช้เข้าใจ
  2. เสนอแนวทางแก้ไข และระบุไฟล์ที่จะต้องแก้ให้ชัดเจน
  3. **หยุดรอการอนุมัติ (Approval) จากผู้ใช้ก่อนเสมอ** เมื่อผู้ใช้ตอบตกลง/อนุญาตแล้วเท่านั้น จึงจะลงมือแก้ไขไฟล์ได้
- ห้ามรัน migration, seed, หรือคำสั่งที่เปลี่ยนแปลงฐานข้อมูล/สภาพแวดล้อม production โดยไม่ได้รับอนุมัติเป็นลายลักษณ์อักษร

## 2. ความปลอดภัยและความสมบูรณ์ของระบบ (System Integrity)
- ห้ามแตะต้องหรือเปลี่ยนลอจิกส่วนอื่นที่ทำงานได้ดีอยู่แล้วโดยไม่จำเป็น
- การตั้งค่า Network / Proxy / API จะต้องไม่ทำให้ระบบเดิม (เช่น Cookie, SSE, Real-time) ได้รับผลกระทบ
- ก่อนแก้ไขไฟล์ config ที่กระทบระบบทั้งหมด (เช่น `.env`, `docker-compose.yml`, `nginx.conf`) ต้องสำรอง (backup) ไฟล์เดิมไว้ก่อนเสมอ

## 3. การจัดการข้อมูลลับ (Secrets Management)
- **ห้าม hardcode** API key, token, password, connection string ลงในโค้ดโดยเด็ดขาด — ต้องใช้ environment variables หรือ secret manager เท่านั้น
- ห้าม commit ไฟล์ `.env`, private key, certificate ลงใน git — ต้องตรวจสอบว่ามีอยู่ใน `.gitignore`
- หากพบ secret หลุดอยู่ในโค้ดหรือ log ให้แจ้งผู้ใช้ทันทีและแนะนำให้ revoke/rotate key นั้น
- ห้ามพิมพ์หรือ log ข้อมูลลับ (token, password, PII) ออกทาง console/log ไฟล์

## 4. การป้องกันช่องโหว่ (Vulnerability Prevention)
- ทุกอินพุตจากผู้ใช้ (form, query param, API body, file upload) ต้องผ่านการ validate และ sanitize ก่อนนำไปใช้เสมอ
- ใช้ parameterized query / ORM เพื่อป้องกัน SQL Injection — ห้าม concatenate string เพื่อสร้าง SQL query
- ป้องกัน XSS โดย escape output ที่แสดงผลบนหน้าเว็บ และหลีกเลี่ยงการใช้ `dangerouslySetInnerHTML` / `eval()` / `innerHTML` กับข้อมูลที่ไม่ผ่านการกรอง
- ตรวจสอบสิทธิ์ (Authorization) ทุกครั้งในฝั่ง Backend ไม่ใช่พึ่งพาแค่การซ่อน UI ฝั่ง Frontend
- จำกัดขนาดและประเภทไฟล์ที่อัปโหลดได้ และสแกน/ตรวจสอบไฟล์ก่อนบันทึก

## 5. การจัดการ Dependencies
- ก่อนเพิ่มไลบรารีใหม่ ให้ตรวจสอบความน่าเชื่อถือ (จำนวนผู้ใช้, การอัปเดตล่าสุด, known vulnerabilities)
- แจ้งเตือนผู้ใช้หากพบ dependency ที่มีช่องโหว่ (CVE) และเสนอแนวทางอัปเดตแบบปลอดภัย (ไม่ทำให้ breaking change โดยไม่แจ้งก่อน)

## 6. Logging & Error Handling
- ข้อความ error ที่ส่งกลับให้ผู้ใช้ (client-facing) ต้องไม่เปิดเผยรายละเอียดระบบภายใน เช่น stack trace, path เซิร์ฟเวอร์, query ฐานข้อมูล
- เก็บ log อย่างเพียงพอสำหรับ debug แต่ต้อง mask ข้อมูลอ่อนไหว (PII, credentials)

## 7. Code Review ก่อน Merge/Deploy
- ก่อน merge หรือ deploy โค้ดที่แก้ไข ต้องสรุปการเปลี่ยนแปลงทั้งหมดให้ผู้ใช้ตรวจสอบอีกครั้ง (diff summary)
- หากการแก้ไขมีผลกระทบต่อความปลอดภัยหรือข้อมูลผู้ใช้ ต้องระบุความเสี่ยงนั้นให้ชัดเจนก่อนขออนุมัติ

## 8. สไตล์การสื่อสาร
- อธิบายด้วยภาษาไทยที่สุภาพ กระชับ ชัดเจน และตรงประเด็น
- เมื่อพบความเสี่ยงด้านความปลอดภัย ให้แจ้งเตือนอย่างตรงไปตรงมาแม้ผู้ใช้จะไม่ได้ถาม
